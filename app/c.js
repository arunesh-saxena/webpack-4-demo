
// import _ from 'lodash';

let com = (file) => {
    let result = 'this is c.js helper';
    // let arrayjoin = _.join(['c', 'file'],'-');
    let arrayjoin = 'hello c.js';
    console.log(`---------${result}------${arrayjoin}------${file}`);
}
export default com; 